# OS files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# Editors / IDEs
.vscode/
.idea/
*.swp
*~

# Environment / secrets
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.ipynb_checkpoints/

# Node
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build / temp output
dist/
build/
*.log
tmp/

# Next.js
# out/ is deliberately NOT ignored: the built static site is committed so the
# HTML is visible in the project and on GitHub. Rebuild with `npm run build`
# and commit the result whenever the source changes.
/.next/
next-env.d.ts
*.tsbuildinfo
/coverage
.vercel
