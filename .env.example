# Copy to .env.local and fill in from your Supabase project:
# Dashboard → Project Settings → API. Both values are compiled into the
# browser bundle, so neither is a secret — the publishable key is meant to be
# public, and row level security is what protects the data behind it.
#
# The same two values must also exist in the GitHub Pages workflow (as repo
# variables) or the deployed site will build without credentials and show the
# "no Supabase credentials" message instead of a sign-in form.

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_key_here
